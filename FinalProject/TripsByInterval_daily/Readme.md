1. The Mapper and Reducer works on interval-src-dst (intermediate data)

output format: daily interval / counts

outfile is the txt file

2. The plot.py file will parse the outfile and output the average daily trips in each time slot
